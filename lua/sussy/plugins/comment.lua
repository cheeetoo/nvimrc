require('Comment').setup({
    -- space between comment line
    padding = true,
    --keep cursor placement
    sticky = true,
    --ignore no lines
    ignore = nil,
    --toggle map
    toggler = {
    }
})
